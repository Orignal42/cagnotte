{% extends 'base.html.twig' %}

{% block title %}Hello PaymentController!{% endblock %}

{% block body %}
     <div class="parallax-container valign-wrapper">
        <div class="section no-pad-bot">
            <div class="container">
                <h1 class="header center white-text light">{{campaign.title}}</h1>
                <div class="row center">
                    <h5 class="header col s12 light">- Organisée par :{{campaign.name}} -</h5>
                </div>
            </div>
        </div>
        <div class="parallax"><img src="{{asset('assets/images/8.jpg')}}" alt="Titre de la campagne"></div>
    </div>

    <div class="container">
        <div class="section">
            <div class="row">
                <div class="col s12 center">
                    <h4>Participez en quelques secondes</h4>
                </div>
            </div>

 <div class="card">
                <div class="card-content">
                    <div class="row">

         {{ include('payment/_form.html.twig') }}

         </div>
         </div>
    </div>  
    </div>       
     </div>    

<script>

    let stripe = Stripe("pk_test_51IudYJE6zq9JtjMeicjOrAVK20EPg4tDenbOCJR8qlP9UOTXSyZNFPEc7m6q1I5Gd8LhewzKZapzBhqSN2A20Pv900JLvBFwXS");
    // The items the customer wants to buy
    let elements = stripe.elements();

    var style = {
    base: {
    // Add your base input styles here. For example:
    fontSize: '16px',
    color: '#32325d',
    },
    };

    // Create an instance of the card Element.
    var card = elements.create('card', {style: style});

        // Add an instance of the card Element into the `card-element` <div>.
    card.mount('#card-element');

</script>

{% endblock %}
